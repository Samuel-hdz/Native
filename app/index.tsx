import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Incio de sesion o registro</Text>
    </SafeAreaView>
  );
};

export default Home;
